# Assets

This directory contains manifests only. Do not commit maps, rosbags, point clouds, model weights, or other large binaries.

Each asset manifest records:

- source URI
- license and attribution
- SHA-256 checksum
- unpack layout
- privacy notes
- benchmark usage

Replace placeholder checksums before using an asset in a real benchmark run.
