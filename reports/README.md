# Reports

Generated static reports are written under `runs/` and are not committed by default.

Use:

```bash
apg report runs/latest/result.json
```

Reports are plain HTML generated from a `RunRecord`.
