api_version: apg/v0
kind: BenchmarkAssets

assets:
  map: assets/maps/sample_map/asset.yaml
  scenario: assets/scenarios/sample_lane_change/asset.yaml
  rosbag: null

notes:
  - MVP dry-run uses manifests only and does not download assets.
